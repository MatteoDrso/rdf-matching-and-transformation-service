# Reference Ontologies (Examples)

Drop the OWL/TTL files here that the Karma model references, so the example
transformation can be reproduced offline.

Expected for the OBOE-aligned `plant_height_vegetative` example:

- `oboe-core.owl` — OBOE 1.2 core ontology
  (https://github.com/NCEAS/oboe → `oboe-core.owl`)

These files are **not committed** by default; place them locally for testing.
